<%= render 'app_bar' %>
<%= render 'index_actions' %>

<%= render layout: 'index_table' do %>
  <% @menu_roots.each do |position, arr| %>
    <%= render partial: 'root_tbody', layout: 'root_tr', locals: { position: position, model: arr[0] } %>
  <% end %>
<% end %>
